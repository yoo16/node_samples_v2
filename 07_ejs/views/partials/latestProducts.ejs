<div class="my-2 space-y-8">
    <h2 class="p-3 text-center text-3xl font-bold">New Products</h2>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-y-10">
        <% latestProducts.forEach(product=> { %>
        <%- include('./product', { product: product }) %>
        <% }) %>
    </div>
</div>