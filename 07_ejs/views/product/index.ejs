<div class="my-2 space-y-8">
    <h2 class="px-3 text-2xl text-center font-bold tracking-tight text-slate-900">Products</h2>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-y-10">
        <% products.forEach(product=> { %>
        <%- include('../partials/product', { product: product }) %>
        <% }) %>
    </div>
</div>