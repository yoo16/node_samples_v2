<main class="container mx-auto max-w-2xl px-4 py-8 mt-16 sm:mt-20">
  <% if (authUser != undefined) { %>
  <!-- 投稿作成フォーム -->
  <%- include('../components/createFeed', { authUser }) %>

  <!-- 投稿リスト -->
  <%- include('../components/feedList', { feeds }) %>
  <% } %>
</main>